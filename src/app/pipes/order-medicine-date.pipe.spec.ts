import { OrderMedicineDatePipe } from './order-medicine-date.pipe';

describe('OrderMedicineDatePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderMedicineDatePipe();
    expect(pipe).toBeTruthy();
  });
});
